import { StrikethroughCompletedTaskDirective } from './strikethrough-completed-task.directive';

describe('StrikethroughCompletedTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new StrikethroughCompletedTaskDirective();
    expect(directive).toBeTruthy();
  });
});
